{
  "name": "atlas-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GraphQL API Server for TrashScanner™",
  "scripts": {
    "dev": "bun --hot src/dev-server.ts",
    "build": "bun build src/prod-server.ts --outdir dist --target bun && cp src/schema/schema.graphql dist/schema.graphql",
    "start": "bun run dist/prod-server.js"
  },
  "dependencies": {
    "@apollo/server": "^5.4.0",
    "atlas-database": "workspace:*",
    "graphql": "^16.12.0",
    "graphql-tag": "^2.12.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
